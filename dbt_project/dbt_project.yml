name: 'nfl_analytics'
version: '1.0.0'
config-version: 2

profile: 'nfl_analytics'

model-paths: ["models"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  nfl_analytics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
